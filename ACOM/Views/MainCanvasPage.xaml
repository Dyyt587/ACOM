<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ACOM.Views.MainCanvasPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ACOM.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:developertools1="using:CommunityToolkit.WinUI.DeveloperTools" xmlns:wuc="using:WinUICommunity"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,0">

        <!-- The same large TextBlock, wrapped in a ScrollViewer. -->
        <ScrollViewer MinHeight="200" MinWidth="200" Height="560"
                      HorizontalAlignment="Stretch" 
                      VerticalAlignment="Stretch"
                  HorizontalScrollBarVisibility="Auto" 
                  VerticalScrollBarVisibility="Auto">
            <Grid >

                <developertools1:AlignmentGrid HorizontalStep="20"
                                  Opacity="0.2"
                                  VerticalStep="20" />
                <wuc:Particle IsPointerEnable="True" Density="3" />

            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
